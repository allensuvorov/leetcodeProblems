/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */
func distributeCoins(root *TreeNode) int {
    // target val = 0
    // source val > 1
    // look for a target with DFS
    // starting from the target,
    // lool for a source with BFS
    // counting distance
    // update values of target and source
}

// corner cases:
// 1 node
// only one side children
